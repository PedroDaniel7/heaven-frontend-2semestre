/** Campos do formulário de cadastro (04-screen-specs.md §6). */
export type RegisterFormData = {
  name: string
  email: string
  password: string
  confirmPassword: string
  acceptedTerms: boolean
}
